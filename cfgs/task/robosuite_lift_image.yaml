# @package _global_
defaults:
  - medium
  - _self_

env:
  env_name: Lift
  has_offscreen_renderer: true
  use_camera_obs: true
  use_object_obs: false

# expert demo directory
demo_dir: ./demonstrations/Lift